// Shared UI grid unit — must match --block in style.css. Every chunky
// tile/button in the app is sized in multiples of this so bars/panels
// share one consistent grid instead of each picking its own height.
export const BLOCK = 37;
